# Avalonia port: ledger

One row per ported WinForms form. **Ported from** is the upstream commit whose behaviour the Avalonia version
reproduces. After merging upstream, list what changed in the WinForms sources since then and re-apply it:

```bash
git log <ported-from>..upstream/master -- <WinForms sources>
```

Then update **Ported from** to the merged upstream commit.

The WinForms forms were deleted in phase 8 (see "Phase 8: WinForms removed" below). Their paths in this table are the
upstream paths, so the `git log` above still lists what changed in them upstream; an upstream change to a deleted
file shows as a modify/delete conflict in the merge, which is resolved by keeping the file deleted and re-applying the
change to the Avalonia version.

| WinForms form | WinForms sources | Ported from | Avalonia view / view model | Routed in | Notes |
|---|---|---|---|---|---|
| `FormAbout` | `src/app/GitUI/CommandsDialogs/FormAbout*.cs`, `EnvironmentInfo*.cs` | `66050831e` | `GitUI.Avalonia/CommandsDialogs/AboutWindow.axaml`, `GitUI.Presentation/CommandsDialogs/AboutViewModel.cs` | `GitUICommands` (`about` verb), `HelpToolStripMenuItem` | Contributor list logic duplicated in `AvaloniaDialogs.TryShowAbout` (a local function in `FormAbout`). |
| `FormRenameBranch` | `src/app/GitUI/CommandsDialogs/FormRenameBranch*.cs` | `66050831e` | `RenameBranchWindow.axaml`, `RenameBranchViewModel.cs` | `GitUICommands.StartRenameDialog` | Git runs in the WinForms `FormProcess`, owned by the Avalonia dialog. |
| `FormCommitTemplateSettings` | `src/app/GitUI/CommandsDialogs/CommitDialog/FormCommitTemplateSettings*.cs` | `66050831e` | `CommitDialog/CommitTemplateSettingsWindow.axaml`, `CommitTemplateSettingsViewModel.cs` | `FormCommit` (commit templates menu → settings) | Settings stored through `AppSettings`/`CommitTemplateItem`, exactly as before. |
| `FormProcess` / `FormStatus` | `src/app/GitUI/HelperDialogs/FormStatus*.cs`, `FormProcess.cs`, `UserControls/PasswordInput*.cs`, `FormStatusOutputLog.cs` | `66050831e` | `HelperDialogs/ProcessWindow.axaml`, `GitUI.Presentation/HelperDialogs/ProcessViewModel.cs` | `FormProcess.ShowDialog`, `FormProcess.ReadDialog`, `FormStatus.ShowErrorDialog` (~70 call sites) | The console control (`IConsoleCommandRunner`) is reused and embedded. The WSL argument rewriting is duplicated in `AvaloniaDialogs.ResolveProcess`; keep it in sync with `FormProcess`'s constructor. Calls with process input and direct `new FormProcess`/`FormRemoteProcess` uses (Push, Pull, Clone) stay on WinForms. |
| `FormRemoteProcess` | `src/app/GitUI/HelperDialogs/FormRemoteProcess*.cs` | `66050831e` | `HelperDialogs/ProcessWindow.axaml` with `ProcessViewModel` (`ExitHandler`, `DataReceived`, `BeforeStart`, `Retry`, `Reset`, `Kill`, `BaseTitle`, `Aborted`); `GitUI/AvaloniaHosting/AvaloniaDialogs.RemoteProcess.cs` (`RunRemoteProcess`, `IRemoteProcessDialog` for the exit handlers, `RemoteProcessDialog`) | `FormRemoteProcess.ShowDialog`, and the Avalonia dialogs running remote commands (push, pull and prune, clone, ls-remote, push and delete of tags, delete of remote branches, sparse working copy) | The questions of PuTTY: a key to load after an authentication error (saved for the remote), a host key to cache, a fingerprint to register (then restarted); the exit handlers of the callers (e.g. a rejected push retried with `--force-with-lease` or after a pull) through `IRemoteProcessDialog`, which also wraps the WinForms form when the port is disabled. The WinForms forms (`FormPush`, `FormPull`, `FormClone`, ...) still use the WinForms form. |
| `FormCommandlineHelp` | `src/app/GitUI/CommandsDialogs/FormCommandlineHelp*` | `66050831e` | `CommandlineHelpWindow.axaml`, `SmallDialogViewModels.cs` | `GitUICommands` (unknown verb) | The command list is read from the WinForms form's `.resx`. |
| `FormAddFiles` | `src/app/GitUI/CommandsDialogs/FormAddFiles*.cs` | `66050831e` | `AddFilesWindow.axaml`, `AddFilesViewModel` | `GitUICommands.StartAddFilesDialog` (`add` verb) | |
| `FormDonate` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormDonate*.cs` | `66050831e` | `DonateWindow.axaml`, `DonateViewModel` | `HelpToolStripMenuItem` | |
| `FormContributors` | `src/app/GitUI/CommandsDialogs/AboutBoxDialog/FormContributors.cs` | `66050831e` | `ContributorsWindow.axaml`, `ContributorsViewModel` | Avalonia About dialog | Strings are constants (not in `English.xlf`). |
| `FormResetChanges` | `src/app/GitUI/CommandsDialogs/FormResetChanges*.cs` | `66050831e` | `ResetChangesWindow.axaml`, `ResetChangesViewModel` | `FormResetChanges.ShowResetDialog` (5 call sites) | |
| `FormDeleteTag` | `src/app/GitUI/CommandsDialogs/FormDeleteTag*.cs` | `66050831e` | `DeleteTagWindow.axaml`, `DeleteTagViewModel` | `GitUICommands.StartDeleteTagDialog` | Remote deletion still uses the WinForms `FormRemoteProcess`, with event scripts through `ScriptHost`. |
| `FormInit` | `src/app/GitUI/CommandsDialogs/FormInit*.cs` | `66050831e` | `InitWindow.axaml`, `InitViewModel` | `GitUICommands.StartInitializeDialog` (`init` verb) | `IsRootedDirectoryPath` duplicated in `InitViewModel`. |
| `FormGoToLine` | `src/app/GitUI/Editor/FormGoToLine*.cs` | `66050831e` | `Editor/GoToLineWindow.axaml`, `GoToLineViewModel` | `FileViewer` (go to line) | |
| `SimplePrompt` | `src/app/GitUI/ScriptsEngine/SimplePrompt*.cs` | `66050831e` | `ScriptsEngine/SimplePromptWindow.axaml`, `SimplePromptViewModel` | `SimplePromptCreator` | Strings are constants (the WinForms prompt is not translated). |
| `FormFilePrompt` | `src/app/GitUI/ScriptsEngine/FormFilePrompt*.cs` | `66050831e` | `ScriptsEngine/FilePromptWindow.axaml`, `FilePromptViewModel` | `FilePromptCreator` | |
| `FormPuttyError` | `src/app/GitUI/FormPuttyError*.cs` | `66050831e` | `HelperDialogs/PuttyErrorWindow.axaml`, `PuttyErrorViewModel` | `FormPuttyError.AskForKey` (clone, remote process) | Key loading still goes through the WinForms `BrowseForPrivateKey` (file dialog + Pageant). |
| `FormBuildServerCredentials` | `src/app/GitUI/HelperDialogs/FormBuildServerCredentials*.cs` | `66050831e` | `HelperDialogs/BuildServerCredentialsWindow.axaml`, `BuildServerCredentialsViewModel` | `BuildServerWatcher.ShowBuildServerCredentialsFormAsync` | Strings are constants (a plain `Form`, never translated). The WinForms form also wrote the fields back on Cancel; the port only writes them on OK. |
| `FormSelectMultipleBranches` | `src/app/GitUI/HelperDialogs/FormSelectMultipleBranches*.cs` | `66050831e` | `HelperDialogs/SelectMultipleBranchesWindow.axaml`, `SelectMultipleBranchesViewModel` | `BranchComboBox` (multiple selection button) | Space toggles the selected item, as in a `CheckedListBox`. |
| `FormChooseTranslation` | `src/app/GitUI/CommandsDialogs/SettingsDialog/Pages/FormChooseTranslation*.cs` | `66050831e` | `SettingsDialog/ChooseTranslationWindow.axaml`, `ChooseTranslationViewModel` | `Program.Main` (first start, through the public `AvaloniaStartupDialogs`), `ChecklistSettingsPage` | A wrap panel of flag buttons replaces the large-icon list view. |
| `FormAvailableEncodings` | `src/app/GitUI/CommandsDialogs/SettingsDialog/FormAvailableEncodings*.cs` | `66050831e` | `SettingsDialog/AvailableEncodingsWindow.axaml`, `AvailableEncodingsViewModel` | `GitConfigSettingsPage` | Encoding list logic duplicated in `AvailableEncodingsViewModel.GetSelectableEncodings`. |
| `FormAddSubmodule` | `src/app/GitUI/CommandsDialogs/SubmodulesDialog/FormAddSubmodule*.cs` | `66050831e` | `AddSubmoduleWindow.axaml`, `AddSubmoduleViewModel` | `FormSubmodules` | Reuses `FormAddSubmodule.LoadRemoteRepoBranches` (made `internal`). The local path is also suggested after Browse, not only while typing. |
| `FormCleanupRepository` | `src/app/GitUI/CommandsDialogs/FormCleanupRepository*.cs` | `66050831e` | `CleanupRepositoryWindow.axaml`, `CleanupRepositoryViewModel` | `GitUICommands.StartCleanupRepositoryDialog` | Path argument logic duplicated in the view model. |
| `FormMergeSubmodule` | `src/app/GitUI/CommandsDialogs/FormMergeSubmodule*.cs` | `66050831e` | `MergeSubmoduleWindow.axaml`, `MergeSubmoduleViewModel` | `FormResolveConflicts` (submodule conflicts) | Checkout branch also requires the local commit (the WinForms form would fail to parse "deleted"). Not covered by an integration test (needs a submodule conflict). |
| `FormCreateWorktree` | `src/app/GitUI/CommandsDialogs/WorktreeDialog/FormCreateWorktree*.cs` | `66050831e` | `CreateWorktreeWindow.axaml`, `CreateWorktreeViewModel` | `GitUICommands.WorktreeCreate` | Reuses `FormCreateWorktree.CreateWorktreeCommand` (made `internal static`). Branches are listed synchronously. |
| `FormOpenDirectory` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormOpenDirectory*.cs` | `66050831e` | `OpenDirectoryWindow.axaml`, `OpenDirectoryViewModel` | `FormOpenDirectory.OpenModule` (dashboard, Start menu) | Reuses `FormOpenDirectory.OpenGitRepository` (made `internal`); `GetDirectories` duplicated in the view model. |
| `BranchComboBox` (control) | `src/app/GitUI/UserControls/BranchComboBox*.cs` | `66050831e` | `Controls/BranchSelector.axaml`, `GitUI.Presentation/UserControls/BranchSelectorViewModel.cs` | Avalonia branch dialogs | The multiple selection opens the Avalonia `SelectMultipleBranchesWindow` directly. |
| `HelpImageDisplayUserControl` (control) | `src/app/GitUI/UserControls/HelpImageDisplayUserControl*.cs` | `66050831e` | `Controls/HelpImageView.axaml`, `HelpImageViewModel.cs` | Avalonia merge dialog | Shares the `HelpIsExpanded<id>` setting. The images are lightened on a dark theme (`ImageLightness`, the port of `LightnessCorrection`). |
| `FormDeleteBranch` | `src/app/GitUI/CommandsDialogs/FormDeleteBranch*.cs` | `66050831e` | `DeleteBranchWindow.axaml`, `DeleteBranchViewModel` | `GitUICommands.StartDeleteBranchDialog` | Worktree handling duplicated in `AvaloniaDialogs.DeleteBranchHost.HandleWorktreeBranches` (reuses `ClassifyWorktreeBranches`); keep in sync. |
| `FormDeleteRemoteBranch` | `src/app/GitUI/CommandsDialogs/FormDeleteRemoteBranch*.cs` | `66050831e` | `DeleteRemoteBranchWindow.axaml`, `DeleteRemoteBranchViewModel` | `GitUICommands.StartDeleteRemoteBranchDialog` | Remote deletion loop duplicated in `DeleteRemoteBranchHost` (Pageant, event scripts, WinForms `FormRemoteProcess`). |
| `FormMergeBranch` | `src/app/GitUI/CommandsDialogs/FormMergeBranch*.cs` | `66050831e` | `MergeBranchWindow.axaml`, `MergeBranchViewModel` | `GitUICommands.StartMergeBranchDialog` | Merge logic duplicated in `MergeBranchHost.Merge`. A merge strategy is only passed while "non-default merge strategy" is checked (the WinForms form also passed a hidden one). |
| `CommitSummaryUserControl` (control) | `src/app/GitUI/UserControls/CommitSummaryUserControl*.cs` | `66050831e` | `Controls/CommitSummaryView.axaml`, `GitUI.Presentation/UserControls/CommitSummaryViewModel.cs` | Avalonia commit dialogs | Fed with a `CommitSummary` record built by `AvaloniaDialogs.ToCommitSummary`. |
| `FormCherryPick` | `src/app/GitUI/CommandsDialogs/FormCherryPick*.cs` | `66050831e` | `CherryPickWindow.axaml`, `CherryPickViewModel` | `GitUICommands.StartCherryPickDialog` (single and multiple revisions) | Choosing another commit opens the WinForms `FormChooseCommit` (revision grid, phase 4). |
| `FormRevertCommit` | `src/app/GitUI/CommandsDialogs/FormRevertCommit*.cs` | `66050831e` | `RevertCommitWindow.axaml`, `RevertCommitViewModel` | `GitUICommands.StartRevertCommitDialog` | Commit message merging duplicated in `RevertCommitHost`. |
| `FormResetCurrentBranch` | `src/app/GitUI/HelperDialogs/FormResetCurrentBranch*.cs` | `66050831e` | `HelperDialogs/ResetCurrentBranchWindow.axaml`, `ResetCurrentBranchViewModel` | `GitUICommands.StartResetCurrentBranchDialog`, `FormReflog`, `RevisionGridControl` | F1 replaces the title bar help button. The "Reset type" caption is not translated (as in WinForms). |
| `FormResetAnotherBranch` | `src/app/GitUI/HelperDialogs/FormResetAnotherBranch*.cs` | `66050831e` | `HelperDialogs/ResetAnotherBranchWindow.axaml`, `ResetAnotherBranchViewModel` | `RevisionGridControl` | Branch ordering duplicated in `TryShowResetAnotherBranch`. The fast-forward check runs synchronously (the WinForms form ran it in the background). |
| `FormArchive` | `src/app/GitUI/CommandsDialogs/FormArchive*.cs` | `66050831e` | `ArchiveWindow.axaml`, `ArchiveViewModel` | `GitUICommands.StartArchiveDialog` | The revision to compare with is shown as a full commit summary. Saving uses the Avalonia save file picker (`IFileDialogService.PickSaveFileAsync`). |
| `CommitPickerSmallControl` (control) | `src/app/GitUI/UserControls/CommitPickerSmallControl*.cs` | `66050831e` | `Controls/CommitPickerView.axaml`, `GitUI.Presentation/UserControls/CommitPickerViewModel.cs` | Avalonia create branch/tag dialogs | Not translated (as in WinForms). Choosing a commit opens the WinForms `FormChooseCommit`. |
| `FormCreateBranch` | `src/app/GitUI/CommandsDialogs/FormCreateBranch*.cs` | `66050831e` | `CreateBranchWindow.axaml`, `CreateBranchViewModel` | `GitUICommands.StartCreateBranchDialog`, `RevisionGridControl`, `FormReflog`, `FormVerify` | The WinForms options (`CheckoutAfterCreation`, `UserAbleToChangeRevision`, `CouldBeOrphan`) are passed as `CreateBranchOptions`. |
| `FormCreateTag` | `src/app/GitUI/CommandsDialogs/FormCreateTag*.cs` | `66050831e` | `CreateTagWindow.axaml`, `CreateTagViewModel` | `GitUICommands.StartCreateTagDialog`, `RevisionGridControl`, `FormVerify` | The message is a plain text box until `EditNetSpell` (spell checking) is ported in phase 5. |
| `FormCheckoutRevision` | `src/app/GitUI/CommandsDialogs/FormCheckoutRevision*.cs` | `66050831e` | `CheckoutRevisionWindow.axaml`, `CheckoutRevisionViewModel` | `GitUICommands.StartCheckoutRevisionDialog` | |
| `GitUI.UserControls.BranchSelector` (control) | `src/app/GitUI/UserControls/BranchSelector*.cs` | `66050831e` | `Controls/LocalRemoteBranchSelectorView.axaml`, `LocalRemoteBranchSelectorViewModel.cs` | Avalonia compare to branch dialog | Renamed to avoid a clash with the `BranchComboBox` port. The "branches containing commits" mode is not ported yet (unused by the ported dialogs). |
| `FormCompareToBranch` | `src/app/GitUI/CommandsDialogs/FormCompareToBranch*.cs` | `66050831e` | `CompareToBranchWindow.axaml`, `CompareToBranchViewModel` | `FormDiff`, `RevisionGridControl` | |
| `FormBisect` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormBisect*.cs` | `66050831e` | `BisectWindow.axaml`, `BisectViewModel` | `FormBrowse`, `InteractiveGitActionControl` | The selected revisions come from the WinForms revision grid (`IRevisionGridInfo`). |
| `FormGoToCommit` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormGoToCommit*.cs` | `66050831e` | `GoToCommitWindow.axaml`, `GoToCommitViewModel` | `RevisionGridMenuCommands.GotoCommitExecute` | Branches and tags are listed before the dialog opens (the WinForms form loaded them in the background). |
| `FormDashboardCategoryTitle` | `src/app/GitUI/CommandsDialogs/BrowseDialog/DashboardControl/FormDashboardCategoryTitle*.cs` | `66050831e` | `DashboardCategoryTitleWindow.axaml`, `DashboardCategoryTitleViewModel` | `UserRepositoriesList` | An invalid name keeps the dialog open (the WinForms OK button closed it regardless). |
| `FormAddToGitIgnore` | `src/app/GitUI/CommandsDialogs/FormAddToGitIgnore*.cs` | `66050831e` | `AddToGitIgnoreWindow.axaml`, `AddToGitIgnoreViewModel` | `GitUICommands.StartAddToGitIgnoreDialog` | File writing duplicated in `AddToGitIgnoreHost.AddPatterns`. |
| `FormCheckoutBranch` | `src/app/GitUI/CommandsDialogs/FormCheckoutBranch*.cs` | `66050831e` | `CheckoutBranchWindow.axaml`, `CheckoutBranchViewModel` | `GitUICommands.StartCheckoutBranch` (all checkout entry points) | The checkout without the dialog (`DoDefaultActionOrShow`) runs the same view model without a window. An unknown branch is highlighted instead of keeping the focus in the list (`Validating`). The auto stash prompt is duplicated in `CheckoutBranchHost`. |
| `FormClone` | `src/app/GitUI/CommandsDialogs/FormClone*.cs` | `66050831e` | `CloneWindow.axaml`, `CloneViewModel` | `GitUICommands.StartCloneDialog` (menus, dashboard, protocol handler) | The suggestions of `OnRuntimeLoad` are duplicated in `AvaloniaDialogs.GetCloneDefaults` (reuses `FormClone.TryExtractUrl`, made `internal`). Cloning still runs in the WinForms `FormRemoteProcess`. The location hint shows the captions without access keys (WinForms showed `[&Destination:]`). |
| `FormRevisionFilter` | `src/app/GitUI/UserControls/RevisionGrid/FormRevisionFilter*.cs` | `66050831e` | `RevisionFilterWindow.axaml`, `RevisionFilterViewModel` | `RevisionGridControl.ShowRevisionFilterDialog` | The `FilterInfo` fields are mapped to and from `RevisionFilterValues` in `TryShowRevisionFilter`; keep in sync when `FilterInfo` changes. |
| `FormFixHome` | `src/app/GitUI/CommandsDialogs/SettingsDialog/Pages/FormFixHome*.cs` | `66050831e` | `SettingsDialog/FixHomeWindow.axaml`, `SettingsDialog/FixHomeDialog.cs` | `FormFixHome.ShowIfUserWant` (startup check), `GitSettingsPage` | `HasGlobalGitConfig` stays in `FormFixHome` (widened to internal); the located-config choice runs when the window opens, as `OnLoad`. |
| `FormUpdates` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormUpdates*.cs`, `ReleaseVersion.cs` | `66050831e` | `BrowseDialog/UpdatesWindow.axaml`, `BrowseDialog/UpdatesDialog.cs` | `FormBrowse.InternalInitialize` (weekly check), `HelpToolStripMenuItem` | The GitHub release lookup and the installer download are duplicated in `AvaloniaDialogs.Updates.cs` (`HttpClient` instead of the obsolete `WebClient`). |
| `FormManageWorktree` | `src/app/GitUI/CommandsDialogs/WorktreeDialog/FormManageWorktree*.cs` | `66050831e` | `ManageWorktreeWindow.axaml`, `ManageWorktreeDialog.cs` | `FormBrowse.manageWorktreeToolStripMenuItem_Click`, `WorktreeTree.ManageWorktrees` | First use of the Avalonia `DataGrid`. |
| `FormSubmodules` | `src/app/GitUI/CommandsDialogs/FormSubmodules*.cs` | `66050831e` | `SubmodulesWindow.axaml`, `SubmodulesDialog.cs` | `GitUICommands.StartSubmodulesDialog` | The splitter position is not persisted (`SplitterManager`); the removal logic is duplicated in `AvaloniaDialogs.WorktreesAndSubmodules.cs`. |
| `SimpleHelpDisplayDialog` | `src/app/GitUI/CommandsDialogs/SettingsDialog/SimpleHelpDisplayDialog*.cs` | `66050831e` | `SettingsDialog/SimpleHelpDisplayWindow.axaml`, `SettingsDialog/SimpleHelpDisplay.cs` | `ScriptsSettingsPage.btnArgumentsHelp_Click` | First modeless window (`AvaloniaDialogHost.Show`); reused while open and closed with its form, as the WinForms field did. |
| `FormReflog` | `src/app/GitUI/CommandsDialogs/FormReflog*.cs` | `66050831e` | `ReflogWindow.axaml`, `ReflogDialog.cs` | `FormBrowse.toolStripMenuItemReflog_Click` | The reflog parsing moved to `ReflogViewModel.Parse`; the actions reuse the ported create branch and reset current branch dialogs. The row under the mouse is selected and a click opens the actions, as in WinForms. |
| `FormRecentReposSettings` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormRecentReposSettings*.cs` | `66050831e` | `BrowseDialog/RecentReposSettingsWindow.axaml`, `BrowseDialog/RecentReposSettingsDialog.cs` | `UserRepositoriesList.mnuConfigure_Click`, `WorkingDirectoryToolStripSplitButton` | `RecentRepoSplitter` (which measures with WinForms `TextRenderer`) runs in the bridge; the caption width previews the combobox width as the WinForms column width did. |
| `FormRemotes` | `src/app/GitUI/CommandsDialogs/FormRemotes*.cs`, `FormRemotesController.cs` | `66050831e` | `RemotesWindow.axaml`, `RemotesDialog.cs` | `GitUICommands.StartRemotesDialog` | The view model uses `IConfigFileRemoteSettingsManager` and `GitHostingRemoteParser` directly (both in GitCommands); the URL history, the fetch after saving, the WinForms `ColorDialog`, PuTTY and the branches are in the bridge. The pull behavior grid is read-only: the tracking remote and merge branch are edited below it (written to the git config as edited, as the WinForms combos did on validation). |
| `GraphRenderer` / `SegmentRenderer` (revision graph drawing) | `src/app/GitUI/UserControls/RevisionGrid/Graph/Rendering/*.cs` | `66050831e` | `Controls/RevisionGrid/RevisionGraphRenderer.cs` | (used by `RevisionGridView`) | The lane geometry is duplicated with Avalonia drawing calls (device-independent units, `DrawingContext`); re-port upstream changes to it. The layout itself is shared through `GitUI.RevisionGraph`. |
| `RevisionGridControl` (grid only) | `src/app/GitUI/UserControls/RevisionGrid/RevisionGridControl.cs`, `RevisionDataGridView.cs`, `Columns/*.cs`, `FilterInfo.cs` | `66050831e` | `Controls/RevisionGrid/RevisionGridView.axaml`, `UserControls/RevisionGrid/RevisionGridViewModel.cs` | `AvaloniaDialogs.ChooseCommit.cs` (`RevisionGridHost`) | First version: graph, references, subject, author, date and commit id; loading as `PerformRefreshRevisions` (refs, `FilterInfo` revision filter, `RevisionReader`), graph layout ahead of the visible rows. Also: the artificial commits (inserted before HEAD; if HEAD is filtered out, first rather than before its listed parents), quick search (`QuickSearchProvider`), multi-selection. Ported since (see the rows of the grid below): stashes as rows, the context menu, the column settings, avatars, build status, hover highlighting and non-relative graying. |
| `FormChooseCommit` | `src/app/GitUI/HelperDialogs/FormChooseCommit*.cs` | `66050831e` | `HelperDialogs/ChooseCommitWindow.axaml`, `HelperDialogs/ChooseCommitDialog.cs` | `FormArchive` (2), `FormCherryPick`, `FormRebase`, `CommitPickerSmallControl`, and the Avalonia cherry-pick/revert/reset/archive and create branch/tag dialogs | Also from `FormDiff` (with artificial commits). Showing the current branch only does not change the main grid's setting, as the WinForms dialog did. |
| `FormFormatPatch` | `src/app/GitUI/CommandsDialogs/FormFormatPatch*.cs` | `66050831e` | `FormatPatchWindow.axaml`, `FormatPatchDialog.cs` | `GitUICommands.StartFormatPatchDialog` | Multi-selection in the Avalonia revision grid, without artificial commits. |
| `FileViewer` (editing mode only) | `src/app/GitUI/Editor/FileViewer*.cs` | `66050831e` | `Editor/TextEditorView.axaml`, `Editor/TextEditorViewModel.cs` | (used by the file editors) | AvaloniaEdit with line numbers, highlighting by extension (AvaloniaEdit's definitions, lightened on dark themes), whitespace markers (`ShowNonPrintingChars`), search (Ctrl+F) and a line to show. The viewer and its diff modes have their own rows. Ported since the audit of 2026-09-25: the options toolbar and an encoding choice in the file editors (they detect the encoding only). |
| `FormGitAttributes` / `FormMailMap` | `src/app/GitUI/CommandsDialogs/FormGitAttributes*.cs`, `FormMailMap*.cs` | `66050831e` | `RepoFileEditorWindow.axaml`, `FileEditorDialogs.cs` (`RepoFileEditorViewModel`) | `GitUICommands.StartEditGitAttributesDialog`, `StartMailMapDialog` | One view model; the forms differ in the file and their strings. |
| `FormEditor` | `src/app/GitUI/CommandsDialogs/FormEditor*.cs` | `66050831e` | `FileEditorWindow.axaml`, `FileEditorDialogs.cs` (`FileEditorViewModel`) | `GitUICommands.StartFileEditorDialog` (also the `fileeditor` verb, git's editor) | The file is read with ICSharpCode's `FileReader` (encoding detection) and saved keeping its byte order mark, as before. |
| `FormViewPatch` | `src/app/GitUI/CommandsDialogs/FormViewPatch*.cs` | `66050831e` | `ViewPatchWindow.axaml`, `ViewPatchDialog.cs` (`ViewPatchViewModel`) | `GitUICommands.StartViewPatchDialog` | The patches are read with `PatchProcessor`, as before; the diff uses the diff mode of `TextEditorView`. |
| `FormSparseWorkingCopy` | `src/app/GitUI/CommandsDialogs/FormSparseWorkingCopy*.cs` (with `FormSparseWorkingCopyViewModel`) | `66050831e` | `SparseWorkingCopyWindow.axaml`, `SparseWorkingCopyDialog.cs` (`SparseWorkingCopyViewModel`) | `GitUICommands.StartSparseWorkingCopyDialog` | The WinForms view model shows the WinForms process dialog, so its logic is ported, not shared; re-port changes to both files. The rules are read and written in the system encoding. |
| `FormGitIgnore` | `src/app/GitUI/CommandsDialogs/FormGitIgnore*.cs`, `GitIgnoreDialog/GitIgnoreModel.cs`, `GitLocalExcludeModel.cs` | `66050831e` | `GitIgnoreEditorWindow.axaml`, `GitIgnoreDialog.cs` (`GitIgnoreEditorViewModel`) | `GitUICommands.StartEditGitIgnoreDialog` (also the `gitignore` verb) | The strings of the edited file keep the ids of the two models. The default patterns are duplicated; the help text is built from them. |
| `FormChangeLog` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormChangeLog*.cs` | `66050831e` | `BrowseDialog/ChangeLogWindow.axaml`, `ChangeLogDialog.cs` | `HelpToolStripMenuItem.ChangelogToolStripMenuItemClick` | Read-only in the text editor, with Markdown highlighting. |
| `FormVerify` | `src/app/GitUI/CommandsDialogs/FormVerify*.cs` | `66050831e` | `VerifyWindow.axaml`, `VerifyDialog.cs` (`VerifyViewModel`) | `GitUICommands.StartVerifyDatabaseDialog` | fsck and its parsing stay in GitUI (`FormVerify.LostObject` and `LostObjectType` made internal, the only upstream edit); the file type guessing is duplicated in the view model. |
| `FormEdit` | `src/app/GitUI/HelperDialogs/FormEdit*.cs` | `66050831e` | `HelperDialogs/TextViewerWindow.axaml`, `VerifyDialog.cs` (`TextViewerViewModel`) | (the view of `FormVerify`) | Only `FormVerify` uses it. |
| Diff line numbers and in-line differences (not a form) | `src/app/GitUI/Editor/Diff/DiffLineNumAnalyzer.cs`, `LinesMatcher.cs`, `DiffHighlightService.cs` (`HighlightAddedAndDeletedLines`, `AddInlineDifferenceMarkers`) | `66050831e` | `GitUI.Presentation/Editor/DiffLinesAnalyzer.cs`, `InlineDiffAnalyzer.cs`, `GitUI.Avalonia/Editor/DiffLineNumberMargin.cs`, `DiffBackgroundRenderer.cs` | (used by the diff mode of `TextEditorView`) | A port for patches without git's colors. `GitUI.Tests/Editor/Diff/AvaloniaDiffPortParityTests.cs` compares both on the sample diffs, so it fails when an upstream change needs re-porting. |
| Git's colors in diffs (not a form) | `src/app/GitUI/Editor/Diff/AnsiEscapeUtilities.cs`, `DiffLineNumAnalyzer.cs` (`IsMovedLine`) | `66050831e` | `GitUI.Presentation/Editor/AnsiEscapeParser.cs`, `DiffLinesAnalyzer.cs`, `GitUI.Avalonia/Editor/DiffColorizer.cs` | (`TextEditorViewModel.LoadDiff` with the theme colors; `HostThemeColors` in the app) | `GitUI.Tests/Editor/Diff/AvaloniaAnsiPortParityTests.cs` compares the colors, the moved lines and the in-line differences with the WinForms viewer. The git word diff appearance is ported (see the diff appearances row). |
| File status list (not a form) | `src/app/GitUI/UserControls/FileStatusList.cs` (`GetNodes`, `GetItemImageKey`, `IsFilterMatch`, `UpdateFileStatusListView`), `FileStatusList.StatusSorter.cs` | `66050831e` | `GitUI.Presentation/UserControls/FileStatusList/FileStatusTree.cs`, `FileStatusListViewModel.cs`, `GitUI.Avalonia/Controls/FileStatusList/FileStatusListView.axaml` | (used by the ported diff dialogs) | `UI.IntegrationTests/AvaloniaHosting/FileStatusListPortParityTests.cs` compares the trees with the WinForms list. The menu items for any diff are ported in `GitUI/AvaloniaHosting/FileStatusListMenuHost.cs` (from `FileStatusList.ContextMenu.cs`, whose `GetSelectionInfo`, `CanResetToFirst` and `CanResetToSecond` are made internal); `FileStatusListMenuParityTests.cs` compares their visibility with the WinForms menu (bound as `RevisionDiffControl` binds it, also with a remembered file). The menu has the items of `FileStatusList.ContextMenu.cs` in the WinForms order, with the visibility of `RevisionDiffController`, `FileStatusListContextMenuController`, `RememberFileContextMenuController` and `UpdateStatusOfMenuItems` (`FileStatusMenuState`): the submodule items (update, reset, stash, commit), stage and unstage (in the commit dialog, its own staging: `StageSelectedAction` / `UnstageSelectedAction`, as the `stage` / `unstage` of `BindContextMenu`), reset chunk of file and interactive add (`git checkout -p` / `git add -p` in a console), cherry-pick changes (main window: all the lines of the diff of the viewer), in the difftool menu "Diff the selected files" (the file selected last is the second), "Diff with the remembered file" and remember first / second (the remembered file is `RememberFileContextMenuController.Default`, shared with WinForms and forgotten when the main window opens another repository), open in Visual Studio (when installed), rename / move (`ISimplePromptCreator`, asked again with the last name until git succeeds or the user cancels), delete (with its confirmation; unstages first), show in the file tree and filter the file (or folder) in the grid (only where bound: `ShowInFileTreeAction` / `FilterFileInGridAction` of the main window; not in the file tree itself), find file (the WinForms `SearchWindow` owned by the Avalonia window, among all the files of the list), add to .gitignore / .git/info/exclude, skip worktree and assume unchanged (check items, with their tooltips), stop tracking, and the user scripts (those of `ScriptEvent.ShowInFileList` in the menu itself, the others under "Run script"; the selected files, or folder, are `SelectedRelativePaths`). The handlers are duplicated from `FileStatusList.ContextMenu.cs` in `FileStatusListMenuHost` (keep them in sync); the items changing the files raise `RefreshRequested` as `RequestRefresh`. "Sort and group by" is hidden in the file tree, as WinForms. Deliberate differences: "Remember First for diff" remembers an inverted copy of the status (WinForms swaps the names of the listed file itself); the items do not show their hotkeys; Visual Studio (and the editor) open without the line of the viewer; the reset hotkey does nothing when "Reset file(s) to" is disabled (WinForms resets e.g. an untracked file). Ported since the audit of 2026-09-25: git grep ("Find in commit files using git-grep", its toggle and the grep results; the tree can show a grep group, nothing produces it), the custom difftools of the difftool items, the captions (First: A / Second: B) of the difftool menu, "select all" and "collapse root folders" of `FileStatusList.TreeContextMenu.cs` (expand / collapse all are ported, for all the nodes), the open submodule item, the settings button of the toolbar (show ignored / untracked / skip-worktree / assume-unchanged files, edit .gitignore, refresh on focus, show the diff for all parents), the A/B filter buttons, the file type icons of the shell, the script hotkeys with the files of the list (only the main window runs the script hotkeys). |
| `FormDiff` | `src/app/GitUI/CommandsDialogs/FormDiff*.cs` | `66050831e` | `DiffWindow.axaml`, `DiffDialog.cs` (`DiffViewModel`) | `RevisionGridControl.ShowFormDiff` (modeless) | The files come from `FileStatusDiffCalculator`, as `FileStatusList.SetDiffsAsync`. |
| `FormSettings` | `src/app/GitUI/CommandsDialogs/FormSettings*.cs`, `SettingsDialog/SettingsTreeViewUserControl.cs`, `SettingsPageBase.cs`, `SettingsPageWithHeader.cs`, `SettingsPageHeader.cs`, `DistributedSettingsPage.cs`, `GitConfigBaseSettingsPage.cs`, `GroupSettingsPage.cs`, `GitUI/SettingControlBindings/*` | `66050831e` | `CommandsDialogs/SettingsDialog/SettingsWindow.axaml` with the page views in `Pages/` (found by name by `SettingsPageLocator`); `GitUI.Presentation/CommandsDialogs/SettingsDialog/SettingsDialog.cs` (`SettingsDialogViewModel`, `SettingsPageViewModel`), `SettingValues.cs` (the bindings), `Pages/*`; `GitUI/AvaloniaHosting/AvaloniaDialogs.Settings.cs` | `GitUICommands.StartSettingsDialog` | The tree with its filter (title and texts, spaces as "and", Enter for the next page found), the groups showing their root page or first child, the header of the levels (effective, local, distributed, global, system, read-only for effective and system; the settings of a level are saved when switching), OK, Cancel, Apply and Discard (debug), the instant save notice, the last page shown again; the pages edit `AppSettings` in the global settings of the dialog (`UsingContainer`) until saved. Ported pages: all of them. `ChecklistSettingsPage` (the root of Git Extensions: the checks and repairs, `AvaloniaDialogs.SettingsChecklist.cs`, the PuTTY paths found with the SSH page); `GeneralSettingsPage`, `AppearanceSettingsPage`, `SortingSettingsPage`, `ColorsSettingsPage` (through `ColorsSettingsPageController`), `AppearanceFontsSettingsPage` (the WinForms `FontDialog`), `ConsoleStyleSettingsPage`, `AdvancedSettingsPage`, `ConfirmationsSettingsPage`, `FormBrowseRepoSettingsPage`, `DiffViewerSettingsPage` (`AvaloniaDialogs.SettingsPagesA.cs`); `GitSettingsPage`, `GitConfigSettingsPage`, `GitConfigAdvancedSettingsPage` (`GitConfigSettingsPageController`), `SshSettingsPage`, `BuildServerIntegrationSettingsPage` (the settings control of the plugin embedded as a child window), `RevisionLinksSettingsPage`, `ShellExtensionSettingsPage` (`AvaloniaDialogs.SettingsPagesB.cs`); `DetailedSettingsPage`, `CommitDialogSettingsPage`, `BlameViewerSettingsPage`; `ScriptsSettingsPage` (the property grid as a form with the same properties, categories and names, the images of the application); `HotkeysSettingsPage` (`ControlHotkeys`, `TextboxHotkey`); `GitRootIntroductionPage`, `PluginRootIntroductionPage`; the plugin pages (`PluginSettingsPage`: a row per setting as `TableSettingsLayout`, the value models of `SettingControlBindingsProvider` incl. password and credentials, the link labels of `PseudoSetting` raising their click, the icons of the plugins). Differences: the checklist is not translated again after choosing a translation (`Translator.Translate`); a plugin setting with its own WinForms binding (`ISetting.CreateControlBinding`) is shown as a note; the git command and linux tools of `GitSettingsPage` are checked when edited only; tool commands of `GitConfigSettingsPage` are suggested after any change of a path; the names and patterns of the revision links apply as typed; the shells of the browse page are replaced when loading (WinForms added them again); five plain check boxes of the confirmations are two-state (the Designer made them three-state, read as checked); numbers out of the WinForms ranges are clamped. Gaps: the embedded build server control may draw over the page when scrolling; the Link and Font icons are lightened on a dark theme (`ImageLightness`). |
| `FormPush` | `src/app/GitUI/CommandsDialogs/FormPush*.cs` | `66050831e` | `CommandsDialogs/PushWindow.axaml`, `GitUI.Presentation/CommandsDialogs/PushDialog.cs` (`PushViewModel`), `GitUI/AvaloniaHosting/AvaloniaDialogs.Push.cs` (`PushHost`) | `GitUICommands.StartPushDialog` (also `pushOnShow`, as `PushAndShowDialogWhenFailed`) | The remote or URL (with the recent URLs and the folder picker), the three tabs (a branch to a remote branch with the default push remote and the tracking branch, `[ All ]`, `HEAD`; the tags; the multiple branches with the ahead/behind data, the exclusive push/force/delete and the menu of the push column), the options (force with lease, force, recursive submodules, tracking reference, pull request), the checks and questions of `PushChanges`, the scripts, the SSH key, pull, manage remotes. The push runs in the WinForms `FormRemoteProcess` with `HandlePushOnExit` (pull or force with lease when rejected), tested with a bare repository in `UI.IntegrationTests`. Deliberate difference: `GetForcePushOption` gives the lease for the branches when "Force with lease" is checked and forces the tags with "Force push" (in `FormPush`, `ForcePushTags` is bound to `ckForceWithLease` and checked first, which would force the branches too when the binding is active). |
| `FormStash` | `src/app/GitUI/CommandsDialogs/FormStash*.cs` | `66050831e` | `StashWindow.axaml`, `StashDialog.cs` (`StashViewModel`) | `GitUICommands.StartStashDialog` | The files as `LoadGitItemStatuses` loads them. Deliberate difference: "There are no stashes." is a placeholder of the message, not its text (which FormStash saves as the message of a new stash). Ported since the audit of 2026-09-25: the hotkeys (next / previous stash, refresh; `StashViewModel.SelectNextStash` exists), the cherry-pick of all changes in the menu (`CherryPickChangesAction` is set by the main window only). |
| `FormCommitDiff` | `src/app/GitUI/HelperDialogs/FormCommitDiff*.cs`, `UserControls/CommitDiff.cs` | `66050831e` | `HelperDialogs/CommitDiffWindow.axaml`, `CommitDiffDialog.cs` (`CommitDiffViewModel`) | `GitUICommands.StartFormCommitDiff`, `RevisionGridControl.ViewSelectedRevisions` (modeless), `BlameControl` (2) | The commit info, the file status list and the viewer, as the `CommitDiff` control composes them. |
| `BlameControl` | `src/app/GitUI/UserControls/BlameControl*.cs` (`BuildBlameContents`, `BuildAuthorLine`, `CalculateBlameGutterData`, the menu), `Editor/BlameAuthorMargin.cs` | `66050831e` | `Controls/Blame/BlameView.axaml`, `BlameMargin.cs`, `GitUI.Presentation/UserControls/Blame/BlameViewModel.cs`, `BlameContentsBuilder.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.Blame.cs` (`BlameHost`) | (used by the blame dialog) | The author lines are a margin of the file's editor (no second viewer to keep in sync); the text builder is ported and `GitUI.Tests/UserControls/AvaloniaBlamePortParityTests.cs` compares it with the WinForms blame. The age colors are inverted in lightness on dark themes (roughly as `AdaptBackColor`). The hosting plugin's menu items are ported (`IBlameContextMenuProvider`). The avatars are drawn beside the age bars (requested at 32 px, scaled to the line height). |
| `FormBlame` | `src/app/GitUI/CommandsDialogs/FormBlame*.cs` | `66050831e` | `CommandsDialogs/BlameWindow.axaml`, `GitUI.Presentation/CommandsDialogs/BlameDialog.cs` | `GitUICommands.RunBlameCommand` (the `blame` verb) | |
| `FormFileHistory` | `src/app/GitUI/CommandsDialogs/FormFileHistory*.cs`, `RevisionGridControl.cs` (`BuildPathFilter`, `ParseFileNames`, `GetRevisionFileName`, `FindRenamesAndCopiesOpts`), `UserControls/RevisionGrid/CopyContextMenuItem.cs` | `66050831e` | `CommandsDialogs/FileHistoryWindow.axaml`, `GitUI.Presentation/CommandsDialogs/FileHistoryDialog.cs` (`FileHistoryViewModel`); `GitUI/AvaloniaHosting/AvaloniaDialogs.FileHistory.cs` | `GitUICommands.RunFileHistoryCommand` (the `filehistory` and `blamehistory` verbs, when `UseBrowseForFileHistory` is off) | The path filter that follows renames is ported into the bridge (`FileHistoryHost.BuildPathFilter`), as is the copy menu of revisions (`GetRevisionCopyItems`); re-port upstream changes of `RevisionGridControl` there. The tabs, the toolbar settings and the grid menu are ported; Ported since the audit of 2026-09-25: the filter toolbar of the grid, the build report tab, the navigate and view menus of the grid, the custom diff tools (the viewers have the encoding choice of `FileViewerView`). |
| `FormCommit` (first part) | `src/app/GitUI/CommandsDialogs/FormCommit*.cs` (`LoadUnstagedOutput`, `Stage`, `Unstage`, `UnstageAllFiles`, `CheckForStagedAndCommit`, `IsCommitMessageValid`, `ResetSoftClick`, `Amend_CheckedChanged`, `UpdateButtonStates`, `CommitMessageToolStripMenuItemDropDownOpening`, `UpdateBranchNameDisplayAsync`, `UpdateAuthorInfo`, `FormatAllText` coloring) | `66050831e` | `CommandsDialogs/CommitDialog/CommitWindow.axaml`, `CommitMessageColorizer.cs`, `GitUI.Presentation/CommandsDialogs/CommitDialog/CommitViewModel.cs`, `ICommitHost.cs`, `CommitStrings.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.Commit.cs` (`CommitHost`) | `GitUICommands.StartCommitDialog`, `StartSquashCommitDialog`, `StartFixupCommitDialog`, `StartAmendCommitDialog` | The unstaged and staged files with the diff, staging (with the partial list updates of `Stage` and `Unstage`), the message (the text beyond the line limits in red), the validations and questions, commit (and push), amend, reset soft, the fixup/squash/amend kinds, the previous messages, the options, the author, the status bar. The templates menu (`commitTemplatesToolStripMenuItem_DropDownOpening`, `ReplaceMessage` with the branch patterns; `PrefixOrReplaceKeyword` in `ConventionalCommits`, tested with the cases of `FormCommitTests`). The event scripts run through `CommitHost` (as the form's `IScriptOptionsForm`). `CommitMessageManager` gets a hidden `Control` for its error boxes. The formatting of `FormatAllText` (the empty second line, the auto-wrap) and the word wrap of the body are ported in `CommitMessageFormatter` (`WordWrapper` is compared by `GitUI.Tests/CommandsDialogs/AvaloniaCommitPortParityTests.cs`); they apply when typing, pasting and setting the message, as one undo step. The hotkeys of `ExecuteCommand` (the "Commit" hotkey settings; `CommitHotkeyCommand` is compared with `FormCommit.Command` by the same parity tests) come before the focused control, as `ProcessCmdKey`: the focus, stage all, refresh, create branch, the difftool, the next/previous file, "add selection to commit message" and the Conventional Commits menu (`OpenConventionalCommitMenu`). The message editor has the spell checking and auto-completion of `EditNetSpell` (the row below). The GPG signing of the options (`gpgSignCommitChanged`; deliberate difference: the key is entered for "Sign with specific GPG", where `FormCommit` shows its box for "Sign with default GPG") and the selection filter (`toolbarSelectionFilter`, throttled 250 ms, the last 10 filters, its hotkey). The lines of the diff are staged, unstaged and reset with the menu and the hotkeys of the file viewer (`SelectedDiff_PatchApplied` rescans the changes). The icons of the templates (as PNG from the host), "Generate a list of changes in submodules" (`CommitHost.GetListOfChangesInSubmodules`). The custom difftool submenus and the settings of the unstaged list (ignored, untracked, skip-worktree, assume-unchanged files) are the file status list's. |
| `PatchGrid` (control) | `src/app/GitUI/UserControls/PatchGrid*.cs` (`GetInteractiveRebasePatchFiles`, `GetRebasePatchFiles`, `GetPatches`, `RefreshGrid`, `SelectCurrentlyApplyingPatch`, `Patches_DoubleClick`), `PatchFile.cs` | `66050831e` | `Controls/PatchGridView.axaml`, `GitUI.Presentation/UserControls/PatchGridViewModel.cs` (`PatchItem` for `PatchFile`); `GitUI/AvaloniaHosting/PatchGridHost.cs` | Avalonia rebase and apply patch dialogs | The reading of the rebase directory is duplicated in `PatchGridHost`; `UI.IntegrationTests/AvaloniaHosting/PatchGridPortParityTests.cs` compares it with the WinForms grid for a stopped rebase and a stopped `git am`. The commit or patch being applied is selected, orange red and scrolled into view; a double click shows the commit or the patch file. The subject (the whole message of a commit during a rebase) is shown on one line, the whole text in its tooltip. Deliberate difference: the commit hash is empty for patches (the WinForms column showed the zero id, but it is hidden for patches anyway). |
| `FormRebase` | `src/app/GitUI/CommandsDialogs/FormRebase*.cs` | `66050831e` | `RebaseWindow.axaml`, `GitUI.Presentation/CommandsDialogs/RebaseDialog.cs` (`RebaseViewModel`); `GitUI/AvaloniaHosting/AvaloniaDialogs.Rebase.cs` (`RebaseHost`) | `GitUICommands.StartRebaseDialog` (all rebase entry points: `StartRebase`, `StartInteractiveRebase`, `StartTheContinueRebaseDialog`, the menus, the left panel, pull, the merge conflict handler) | The options with their exclusions, the specific range (the commit choice of `btnChooseFromRevision_Click` is in `RebaseHost`, with the Avalonia commit chooser), the rebase started immediately, continue (and continuing again while `CanContinueAction`), skip (the skipped commits kept in a static list, as `FormRebase.Skipped`), abort, edit todo, solve conflicts, add files, commit, the default button between `>` `<` with the focus, the yellow frame of the solve conflicts button. As `FormRebase` (`enablePositionRestore: false`), the position is not restored. |
| `FormApplyPatch` | `src/app/GitUI/CommandsDialogs/FormApplyPatch*.cs` | `66050831e` | `ApplyPatchWindow.axaml`, `GitUI.Presentation/CommandsDialogs/ApplyPatchDialog.cs` (`ApplyPatchViewModel`); `GitUI/AvaloniaHosting/AvaloniaDialogs.ApplyPatch.cs` (`ApplyPatchHost`) | `GitUICommands.StartApplyPatchDialog` (the `apply`/`applypatch` verbs, the browse menu, the merge conflict handler, `InteractiveGitActionControl`) | A patch file (mailbox or raw diff, `IsDiffFile` in the view model) or a directory of patches (`GitModule.ApplyPatch`), ignore whitespace and sign-off (saved as they change), skip, resolved, abort, solve conflicts, add files, the framed default button. The patch file is picked with `IFileDialogService.PickFileAsync` (title and `*.patch` filter), the directory with `PickFolderAsync`. Deliberate differences: the WinForms Apply button has `DialogResult.OK`, so the form closed after every click (even with no patch selected, or when git stopped at a conflict); the port closes only as `Apply_Click` intends (when no patch is being applied any more). `FormApplyPatchLoad` is not subscribed to in WinForms (all buttons start enabled); the port runs its `EnableButtons` when the dialog opens. |
| `EditNetSpell` (not a form) | `src/app/GitUI/SpellChecker/EditNetSpell.cs` (`CheckSpelling`, `MarkLines`, `SpellCheckContextMenuOpening`, `AddWordSuggestions`, `AddDictionaries`, `LoadDictionary`, `UpdateOrShowAutoComplete`, `AcceptAutoComplete`, the key handlers), `SpellCheckEditControl.cs`, `WordAtCursorExtractor.cs`, `AutoCompletion/AutoCompleteWord.cs` | `66050831e` | `GitUI.Avalonia/Editor/SpellCheckController.cs`; `GitUI.Presentation/SpellChecker/SpellCheckViewModel.cs`, `ISpellCheckHost.cs`, `WordAtCursor.cs`, `SpellCheckStrings.cs`; `GitUI/AvaloniaHosting/SpellCheckHost.cs` (NetSpell, the auto-completion providers) | The commit message of `CommitWindow` | The check 250 ms after a change (not for less than 4 characters, more than 5000 or without dictionary), the waves under the misspelled words and the marks of the ill-formed lines (50/0/72 characters), the context menu (the suggestions with add, ignore and remove, the edit items, the dictionaries of the repository, the two options; `FormCommit` inserts the word wrap before the first separator), the right click moving the cursor, the auto-completion (after typing a letter, 200 ms; Ctrl+Space, which accepts a single word) in the completion window of AvaloniaEdit. `WordAtCursorExtractor` and `AutoCompleteWord.Matches` are compared, and the NetSpell operations tested, by `GitUI.Tests/SpellChecker/AvaloniaSpellCheckPortTests.cs`. Deliberate difference: "Ignore word" ignores the word in the editor (`Spelling.IgnoreAllWord`); `EditNetSpell` calls `Spelling.IgnoreWord`, which only skips it until the next check. The paste replacing vertical tabs (`TextPasted`), the double click selecting the word of `WordAtCursorExtractor`, Up / Down wrapping around the list; Shift+Enter is a line feed in AvaloniaEdit already. The IME composition checks are not needed: AvaloniaEdit has no preedit (`SupportsPreedit` is false), the input method composes in its own window and the document gets the committed text only. |
| File viewer changes (not a form) | `src/app/GitUI/GitUIExtensions.cs` (`ViewChangesAsync`), `Editor/FileViewer.cs` (`ViewGitItemAsync`, `ViewFileAsync`, `ViewItemAsync`, `GetExtraDiffArguments`, `GetUpdateTreeId`) | `66050831e` | `GitUI/AvaloniaHosting/FileViewerHost.cs`, `GitUI.Presentation/Editor/FileViewerViewModel.cs`, `GitUI.Avalonia/Editor/FileViewerView.axaml` | (used by the ported diff dialogs) | The toolbar (whitespace, context lines, entire file, encoding, next/previous change). The context menu (`SetVisibilityDiffContextMenu`): stage/unstage/reset of the selected lines (`StageSelectedLines`, `ResetNoncommittedSelectedLines`, `ApplySelectedLines` for commits, with `SupportLinePatching`, `LinePatchingBlocksUntilReload` and `PatchApplied`, tested with a repository in `UI.IntegrationTests`), the copies (without the diff prefixes, the patch, the new and old versions), the options, find (the search panel of AvaloniaEdit), go to line. The hotkeys of the "FileViewer" settings (`FileViewerHotkeyCommand` compared with `FileViewer.Command` by `GitUI.Tests/Editor/AvaloniaFileViewerPortParityTests.cs`); as `ProcessCmdKey`, the hotkeys of the focused viewer come before those of the dialog (`IHotkeyControl`). Ported since: range diffs, git grep results, difftastic, the git word diff, the hex dump of binary files, the syntax highlighting of diffs, "Diff appearance", "Treat all files as text", open with difftool on F3 without a search. Ported since the audit of 2026-09-25: the automatic continuous scroll (the setting only), the next / previous occurrence hotkeys (declared, not handled). |
| The diff appearances of `FileViewer` | `src/app/GitUI/Editor/FileViewer.cs`, `Editor/Diff/*HighlightService.cs`, `DiffLineNumAnalyzer.cs`, `DiffViewerLineNumberControl.cs` | `66050831e` | `GitUI.Presentation/Editor/DiffViewMode.cs`, `DifftasticAnalyzer.cs`, `GrepAnalyzer.cs` (with `RangeDiffAnalyzer`), `DiffLinesAnalyzer.cs`, `FileViewerViewModel.cs`; `GitUI.Avalonia/Editor/DiffLineNumberMargin.cs`, `FileViewerView.axaml`; `GitUI/AvaloniaHosting/FileViewerHost.cs` | every Avalonia viewer | Git word diff, difftastic (with the width of the viewer), range diff, grep, combined and fixed diffs, syntax highlighting of diffs, the Diff appearance submenu, treat all files as text; the git arguments are shared with the WinForms viewer (static helpers of `FileViewer`). Ported since the audit of 2026-09-25: next/previous occurrence of the selection, automatic continuous scroll, jumping to the first change after loading (and keeping the position when a file reloads, `CurrentViewPositionCache`), the range-diff path filter of the main window. The hex dump of binary files is ported (AvaloniaHex). |
| Commit info (not a form) | `src/app/GitUI/CommitInfo/CommitInfo.cs` (`ReloadCommitInfo`, `UpdateRevisionInfo`, `GetSortedTags`; `TagsComparer` made internal), `CommitInfoHeader.cs` | `66050831e` | `GitUI/AvaloniaHosting/CommitInfoHost.cs`, `GitUI.Presentation/UserControls/CommitInfoViewModel.cs`, `GitUI.Avalonia/Controls/CommitInfoView.axaml`, `XhtmlTextBlock.cs` | (used by the ported commit dialogs) | The renderers (`CommitDataHeaderRenderer`, `CommitDataBodyRenderer`, `RefsFormatter`, `LinkFactory`) are used as they are; `XhtmlTextBlock` shows their XHTML as `RichTextBoxXhtmlSupportExtension` does. The avatar (`CommitInfoHeader.LoadAuthorImage`, from `AvatarService`) and the context menu (copy link, copy commit info, the settings of the refs, notes; `commitInfoContextMenuStrip`) are ported. Ported since the audit of 2026-09-25: the menu of the avatar (provider, fallback, cache). |
| `FormBrowse` (first part; only with `GE_AVALONIA=all,FormBrowse`) | `src/app/GitUI/CommandsDialogs/FormBrowse*.cs`, `Menus/StartToolStripMenuItem.cs`, `ToolsToolStripMenuItem.cs`, `HelpToolStripMenuItem.cs` | `66050831e` | `CommandsDialogs/BrowseDialog/BrowseWindow.axaml`, `GitUI.Presentation/CommandsDialogs/BrowseDialog.cs` (`BrowseViewModel`, `IBrowseHost`, `BrowseCommand`, the menus as data); `GitUI/AvaloniaHosting/AvaloniaDialogs.Browse.cs` | `GitUICommands.StartBrowseDialog` (the start of the application and the `browse` verb) | The revision grid (all branches, the artificial commits, multiple selection), the commit tab (commit info) and the diff tab (the files of the diffs of the selected revisions and the viewer, as `RevisionDiffControl`); the Start, Repository, Commands, Tools and Help menus and the main toolbar (refresh, branch, commit, pull with its actions, push, stashes, file explorer, git bash, settings) with the handlers of `FormBrowse`; the title and the refresh after the dialogs and on `PostRepositoryChanged`; another repository (open, clone, init) opens in a new window. The application loop runs until the last main window closes. All of these were ported since (see the rows below): the left panel, the dashboard, the file tree, console, GPG and build report tabs, the filters toolbar, the grid context menu, the recent and favourite repositories, the plugins menu, the scripts, the submodules and worktrees buttons, the commit info position. |
| `RevisionGridControl` (context menu, navigation, columns) | `src/app/GitUI/UserControls/RevisionGrid/RevisionGridControl.cs` (`ContextMenuOpening`, the `*_Click` handlers), `RevisionGridMenuCommands.cs`, `CopyContextMenuItem.cs`, `NavigationHistory.cs`, `ParentChildNavigationHistory.cs` | `66050831e` | `GitUI.Presentation/Services/MenuModel.cs`, `UserControls/RevisionGrid/RevisionGridViewModel.Navigation.cs`, `RevisionGridMenuStrings.cs`; `GitUI.Avalonia/Hosting/MenuModelRenderer.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.RevisionGridMenu.cs` (`RevisionGridMenuBuilder`) | the Avalonia main window | The menu is built when it opens from the same conditions as `ContextMenuOpening`; re-port upstream changes of it into `RevisionGridMenuBuilder`. The View menu has all the items of `CreateViewMenuCommands`: also the advanced filter, the session checkpoints, the superproject tags and branches (the labels of `DrawSuperprojectRefs` / `DrawSuperprojectInfo`, read with the revisions by `RevisionGridHost`; differences: all after the references, and a reference also of the submodule is not marked), the commit message body (when git log read it; WinForms also loads it later), the sort orders and "Save current view settings as default"; no group headers. Ported since the audit of 2026-09-25: the custom difftools submenu. |
| `RevisionGridControl` (stashes, build status, avatars, notes, highlighting, ref label menu, hotkeys) | `src/app/GitUI/UserControls/RevisionGrid/RevisionGridControl.cs` (`PerformRefreshRevisions`, `GetStashRevs`, `ExecuteCommand`), `Columns/AvatarColumnProvider.cs`, `BuildStatusColumnProvider.cs`, `GitNotesColumnProvider.cs`, `BuildServerWatcher.cs`, `HoverHighlightCalculator.cs` | `66050831e` | `GitUI.Presentation/UserControls/RevisionGrid/RevisionGridViewModel.Columns.cs`, `RevisionGridView`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseBuildServer.cs` (`GridBuildServerWatcher`), `AvaloniaDialogs.ChooseCommit.cs`, `AvaloniaDialogs.RevisionGridMenu.cs` | the Avalonia grids | The build server watcher is copied from `BuildServerWatcher` (tied to the WinForms grid): re-port upstream changes. Deliberate difference: the index/untracked helper commits of the newest stash are hidden too. Ported since the audit of 2026-09-25: the favicon of the build report tab, reloading avatars when their cache is cleared (`ClearAvatars` exists, the settings page does not call it), the "select next fork point" hotkey (declared, not handled), ref label tooltips and double-click, nested remote/ahead-behind labels. |
| `BuildReportTabPageExtension` (the build report tab of the main window) | `src/app/GitUI/CommandsDialogs/BuildReportTabPageExtension.cs` | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.BuildReport.cs`, `GitUI.Avalonia/CommandsDialogs/BrowseDialog/BrowseWindow.BuildReport.cs`; `GitUI/AvaloniaHosting/BrowseWebViews.cs`, `AvaloniaDialogs.BrowseBuildServer.cs` | the Avalonia main window | The report in WebView2 (`WebView2BrowseWebView`, Core API only: a Win32 child window hosts the controller, whose bounds follow `WM_SIZE`; the user data in `%LOCALAPPDATA%\GitExtensions\WebView2`; navigations before the controller is ready are queued; links opening a new window go to the default browser), embedded as a native view, or the "Open report" link. Without the WebView2 runtime (`GetAvailableBrowserVersionString` throws) the WinForms `WebBrowserControl` is embedded instead. The WinForms `BuildReportTabPageExtension` keeps `WebBrowserControl`. |
| `RepoObjectsTree` (the left panel of the main window) | `src/app/GitUI/LeftPanel/**/*.cs` | `66050831e` | `Controls/LeftPanel/LeftPanelView.axaml`, `GitUI.Presentation/UserControls/LeftPanel/*.cs` (`LeftPanelViewModel`, the trees and nodes); `GitUI/AvaloniaHosting/AvaloniaDialogs.LeftPanel.cs` (`AttachLeftPanel`) | the Avalonia main window | The six trees with their nodes, selection, multi-selection, search, context menus, settings and hotkeys; "filter for selected" through the filter toolbar. The user scripts of the menu of a visible local branch ("Run script" and the scripts of the grid menu, run with the options of the main window), Alt+click selecting the related branch, the width and the collapsed state saved with the splitters of the main window. The icons that `RepoObjectsTree` lightens on a dark theme (Branch, the eyes, RemoteEnableAndFetch, collapse and expand) are lightened by `ImageLightness`. |
| `FormBrowse` file tree tab (`TreeTabPage`, `RevisionDiffControl` in file tree mode) | `src/app/GitUI/CommandsDialogs/FormBrowse.cs` (`FillFileTree`), `RevisionDiffControl.cs`, `UserControls/FileStatusList.cs` (`isFileTreeMode`) | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.FileTree.cs`, `FileStatusListViewModel.IsFileTreeMode`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseFileTree.cs` | the Avalonia main window | The files come from `FileStatusDiffCalculator` in file tree mode (`SetGrep("", fileTreeMode: true)`), loaded when the tab is shown; the selected file stays selected in the next revision. The menu of the files (`FileStatusListMenuHost`, see the file status list) with the hotkeys of the diff tab (`ExecuteRevisionDiffCommand(fileTree: true)`); its changes load an artificial commit again. "Show in file tree" of the diff tab selects the file or the folder once the tree is loaded. The blame of the file tree and of the diff tab (`BrowseViewModel.Blame.cs`, `AvaloniaDialogs.BrowseBlame.cs`). Ported since the audit of 2026-09-25: git grep in the tree (the hotkeys are declared, not handled). |
| `RevisionGpgInfoControl` (the GPG tab of the main window) | `src/app/GitUI/CommandsDialogs/RevisionGpgInfoControl*.cs`, `FormBrowse.FillGpgInfoAsync` | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.Gpg.cs`, the `gpgTab` of `BrowseWindow.axaml`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseGpg.cs` | the Avalonia main window | Verified when the tab is shown (`GpgInfoProvider`), "Loading data..." meanwhile; the tag row only with a tag. |
| The console tab of `FormBrowse` (`FillTerminalTab`) | `src/app/GitUI/CommandsDialogs/FormBrowse.cs` (`FillTerminalTab`, `ChangeTerminalActiveFolder`) | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.Console.cs`, `GitUI.Avalonia/Controls/EmbeddedNativeViewPresenter.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseConsole.cs` | the Avalonia main window | The WinForms `IConsoleShellRunner` (ConEmu, Mintty) as a child window, created when the tab is first selected. Difference: another repository gets a new view model with a new terminal, whose shell starts in the new working directory when the tab is shown (WinForms keeps the shell and changes its directory, `ChangeTerminalActiveFolder`). |
| `FilterToolBar` (the filters of the main window) | `src/app/GitUI/UserControls/FilterToolBar*.cs`, `RevisionGridControl.cs` (the `IRevisionGridFilter` members) | `66050831e` | `Controls/RevisionGrid/FilterToolBarView.axaml`, `GitUI.Presentation/UserControls/RevisionGrid/FilterToolBarViewModel.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseFilter.cs` (`BrowseGridFilter`) | the Avalonia main window | The window keeps one `FilterInfo` (as the grid's `_filterInfo`), also used by the View menu of the grid; the command line `RevFilter`/`PathFilter` are applied as `InitFilters`. The hotkeys (focus filter, the branch modes) are ported. The branch list is updated and dropped down while typing. Deliberately not: following renames in the path filter (`BuildPathFilter` of a single file; only the file history does). |
| The Plugins, repository host, Navigate and View menus of `FormBrowse` | `src/app/GitUI/CommandsDialogs/FormBrowse.cs` (`RegisterPlugins`, `UpdatePluginMenu`, the repository host handlers), `BrowseDialog/FormBrowseMenus.cs` | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.Plugins.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowsePlugins.cs` | the Avalonia main window | The plugins load once in the background (`PluginRegistry.InitializeAll`) and are registered for each repository; the menus are built again when they are loaded. Navigate and View are the items of the grid menu (`RevisionGridMenuBuilder`). The Toolbars submenu of View is ported with the toolbar (see the toolbar row). The hotkeys of the Navigate and View menus are shown and work while the grid has the focus, as in WinForms. Deliberately not: toolbar items of plugins (the plugin API has no toolbar, and no bundled plugin reaches into the WinForms one). |
| The main toolbar and the hotkeys of `FormBrowse` | `src/app/GitUI/CommandsDialogs/FormBrowse.cs` (`InitCountArtificial`, `ExecuteCommand`, `LoadUserMenu`, `userShell_Click`, `toolStripWorktrees_DropDownOpening`), `FormBrowse.InitMenusAndToolbars.cs`, `Menus/WorkingDirectoryToolStripSplitButton.cs` | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.Toolbar.cs`, `BrowseViewModel.WorkingDirectoryStatus.cs`, `BrowseViewModel.Hotkeys.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseToolbar.cs`, `AvaloniaDialogs.BrowseStatus.cs` | the Avalonia main window | The commit button with the status of the working directory (`GitStatusMonitor`), the working directory button, the worktrees button, the shells, the scripts toolbar, and the "Browse" hotkeys with the scripts' ones. The submodules button (`toolStripButtonLevelUp`: the superproject, the submodules with their status, update all/current; `AvaloniaDialogs.BrowseSubmodules.cs`). The layout buttons (`toggleSplitViewLayout`: the tabs below the grid; `menuCommitInfoPosition`: the commit info in its tab or left or right of the grid, moved between them; saved in `AppSettings`; `BrowseViewModel.Layout.cs`, `AvaloniaDialogs.BrowseLayout.cs`); the splitters are saved when the window closes with the keys of `SplitterManager` (`FormBrowse.MainSplitContainer_Distance`, ...: the left panel with whether it is shown, the tabs, the output history panel, the file lists of the diff and file tree tabs; `BrowseWindow.Splitters.cs`), not the commit info beside the grid. Escape clears an applied filter and does not close the window (`FormBrowse.CancelButtonClick`). The default pull action (`setDefaultPullButtonActionToolStripMenuItem`: the image and tooltip of the pull button), the fetch and pull shortcut buttons (hidden by default), and the Toolbars menu (at the end of the View menu and on the free space of the toolbar: each item of the Standard toolbar shown or hidden, saved as `formbrowse_toolbar_visibility_<name>`, the separators adapted; `BrowseViewModel.ToolbarItems.cs`, `AvaloniaDialogs.BrowseToolbarItems.cs`). Differences: a toolbar is shown or hidden by the first item of its submenu (a menu item with a submenu cannot be checked in Avalonia); the menu closes after each click; the items of the Filters toolbar are shown or hidden too (its branch and text filter groups saved by their group names), the Scripts toolbar as a whole only (its buttons follow the settings of the scripts). The search box of the working directory menu (the recent repositories filtered as typed). The "BrowseDiff" hotkeys of the diff and file tree tabs (`RevisionDiffControl.ExecuteCommand`; `BrowseViewModel.RevisionDiffHotkeys.cs`, `HotkeyGrid`): file history, blame, the difftools, edit, open (temp file, working directory file, with), reset to the first revision, show in the file tree, filter the file in the grid, go to the first or last parent; as the items of the context menu, nothing when the item is disabled. Also delete, stage / unstage, rename / move, add to .gitignore, Visual Studio, find file and select the first group (all the files without diff groups); show in the file tree and filter in the grid run their menu items (the folder, or all the selected files, as `FilterFileInGrid`). The items of the menu of the files refresh as `RevisionDiffControl.RequestRefresh`: the status of the working directory at once (`IBrowseStatusHost.RequestStatusRefresh`, `RefreshGitStatusMonitor`) and the diffs of the selected artificial commits (`FileStatusListViewModel.RefreshGroups`, as `RefreshArtificial`: the selected files stay selected, else the file after them, else the first), also after a line patch of the viewer (`DiffText_PatchApplied`). Ported since the audit of 2026-09-25: the git grep hotkeys (no git grep in the Avalonia list). The taskbar (`AvaloniaDialogs.BrowseTaskbar.cs`): the jump list with its thumbnail toolbar (commit, pull, push, close all windows) created once the window is activated and enabled for a valid repository, the repository added to the recent ones of the jump list, the overlay dot of the status of the working directory, and the "close all instances" message closing the window (not a modal dialog over it first, as WinForms). The counts of the changes of the artificial commits in the grid (`ShowGitStatusForArtificialCommits`: `UpdateArtificialCommitCount` from the `GitStatusMonitor` of the commit button; `RevisionGridViewModel.ArtificialCommits.cs` duplicates `ArtificialCommitChangeCount`, and its tooltip, untranslated as in WinForms); the subject of an artificial commit is text, not a reference label. |
| `OutputHistoryControl`, `OutputHistoryTabController` (the Output tab of the main window) | `src/app/GitUI/UserControls/OutputHistory*.cs` | `66050831e` | `GitUI.Presentation/CommandsDialogs/BrowseViewModel.OutputHistory.cs`, the `outputHistoryTab` of `BrowseWindow.axaml`; `GitUI/AvaloniaHosting/AvaloniaDialogs.BrowseOutputHistory.cs` | the Avalonia main window | A tab, or with `ShowOutputHistoryAsTab` off the panel below the left panel (`OutputHistoryPanelController`: shown or hidden by the Output hotkey and saved as `OutputHistoryPanelVisible`; alone in the left column when the left panel is hidden); its height is not synchronized with the diff file list as in WinForms. The links of the history are not clickable. |
| The plugin forms: `CreateLocalBranchesForm`, `DeleteUnusedBranchesForm`, `FindLargeFilesForm`, `GourceStart`, `ProxySwitcherForm`, `ReleaseNotesGeneratorForm`, `FormGitStatistics`, `FormImpact` (with `ImpactControl`), `TeamCityBuildChooser` | `src/plugins/**` | `66050831e` | each plugin's `*ViewModel.cs`, `*Window.axaml`, `*Dialog.cs` (in the plugin, so that their strings stay in English.Plugins.xlf), `PieChartView.cs`, `ImpactGraphView.cs`; `src/plugins/AvaloniaPort.props`; `GitUI/AvaloniaHosting/AvaloniaPluginDialogs.cs` (the public entry point of the plugins) | each plugin's `Execute` (TeamCity: its settings button) | The logic classes of the plugins are reused; the batch file of FindLargeFiles and the avatar download of Gource are copied (a parity test covers the batch file). The pie charts have no gradual shadow; the colors of the impact graph come from the hash code of the author, as in WinForms. |
| `ForkAndCloneForm`, `CreatePullRequestForm`, `ViewPullRequestsForm` | `src/app/GitUI/CommandsDialogs/RepoHosting/*.cs` | `66050831e` | `GitUI.Presentation/CommandsDialogs/RepoHosting/*`, `GitUI.Avalonia/CommandsDialogs/RepoHosting/*`; `GitUI/AvaloniaHosting/AvaloniaDialogs.RepoHosting.cs` | `GitUICommands.StartCloneForkFromHoster`, `StartPullRequestsDialog`, `StartCreatePullRequest` | The discussion of a pull request is shown as text, not HTML. Refresh and Post comment now work (the WinForms buttons had no handlers). Ported since the audit of 2026-09-25: moving to the next file when scrolling past the end of a diff. |
| `Dashboard` / `UserRepositoriesList` (the dashboard of the main window) | `src/app/GitUI/CommandsDialogs/BrowseDialog/DashboardControl/*.cs` | `66050831e` | `CommandsDialogs/BrowseDialog/DashboardView.axaml`, `GitUI.Presentation/CommandsDialogs/BrowseDialog/DashboardDialog.cs` (`DashboardViewModel`); `GitUI/AvaloniaHosting/AvaloniaDialogs.Dashboard.cs` | the Avalonia main window without a valid repository | The start and contribute links, the recent and category groups with their tiles, search, menus of the tiles and groups, drop of a folder. The Develop and Translate icons are lightened on a dark theme. The background image is tiled, the arrow keys move between the groups, and the git hoster links are added once the plugins are loaded. |
| `FormBrowse.SetGitModule` / `SetWorkingDir` (switching repositories), the recent and favourite repositories menus | `src/app/GitUI/CommandsDialogs/FormBrowse.cs`, `Menus/StartToolStripMenuItem.cs`, `GitUICommands.WorktreeSwitch` | `66050831e` | `GitUI/AvaloniaHosting/AvaloniaDialogs.Browse.cs` (`BrowseSession`) | the Avalonia main window, `GitUICommands.WorktreeSwitch` | Each repository gets new commands and a new view model in the same window; the menus reuse `IRepositoryHistoryUIService`. The superproject and the submodules are opened in the window by the submodules button (see the toolbar row). The revisions to select after a switch (`IBrowseRepo.SetWorkingDir`: `FirstId`, then `SelectedId`, as `GetToBeSelectedRevisions`; also from the command line). |
| `FormGitCommandLog` | `src/app/GitUI/CommandsDialogs/BrowseDialog/FormGitCommandLog*.cs` | `66050831e` | `CommandsDialogs/BrowseDialog/GitCommandLogWindow.axaml`, `GitUI.Presentation/CommandsDialogs/BrowseDialog/GitCommandLogDialog.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.GitCommandLog.cs` | `FormGitCommandLog.ShowOrActivate` | The modeless single window (activated if open), both tabs, the menus with Ctrl+S/C/L, always on top, word wrap, the call stacks, save to file. Differences: the list refreshes on the next UI tick; it closes with its owner only when the owner is a WinForms form. |
| `FormFindInCommitFilesGitGrep` | `src/app/GitUI/Editor/FormFindInCommitFilesGitGrep*.cs` | `66050831e` | `Editor/FindInCommitFilesGitGrepWindow.axaml`, `GitUI.Presentation/Editor/FindInCommitFilesGitGrepDialog.cs` | (nothing since phase 8) | The git grep prompt. Gap: nothing shows it since the WinForms list was removed in phase 8 (its host went with it); the Avalonia file status list has no git grep yet. Differences: unchecking "Show" clears the search at once. |
| `FormQuickItemSelector` | `src/app/GitUI/UserControls/RevisionGrid/FormQuickItemSelector*.cs` | `66050831e` | `Controls/RevisionGrid/QuickItemSelectorWindow.axaml`, `GitUI.Presentation/UserControls/RevisionGrid/QuickItemSelectorDialog.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.QuickItemSelector.cs` | `RevisionGridControl.InitiateRefAction`, `ScriptOptionsParser.AskToSpecify` | The ref and string pickers at the grid cell (`DialogWindow.StartupScreenPosition`). Difference: cancelling returns nothing (WinForms returned the highlighted item on Escape). |
| `FormLog` | `src/app/GitUI/CommandsDialogs/FormLog*.cs` | `66050831e` | `CommandsDialogs/LogWindow.axaml`, `GitUI.Presentation/CommandsDialogs/LogDialog.cs`; `GitUI/AvaloniaHosting/AvaloniaDialogs.Log.cs` | `GitUICommands.StartCompareRevisionsDialog` (the `viewdiff` verb) | The grid (default filter, the uncommitted changes, multiple selection, the current checkout selected), the files and the diff; double click opens the commit diff or the file history. Its strings use the `FormDiff` category, the `Name` of `FormLog`. Not ported: the grid context menu, moving to the next file at the end of the diff. |
| `FindAndReplaceForm` | `src/app/GitUI/Editor/FindAndReplaceForm*.cs` | `66050831e` | the AvaloniaEdit search panel of `TextEditorView`/`FileViewerView`, `Editor/SearchPanelLocalization.cs`, `GitUI.Presentation/Editor/FindAndReplaceStrings.cs` | Ctrl+F, Ctrl+H, F3 in the Avalonia editors | Match case, whole word, regex, highlight all and replace (all) in editable files were covered; added: the search starts with the selection or the word at the caret, F3/Shift+F3 after the panel closed, no replace in read-only text, the translated texts (through a private field of AvaloniaEdit, checked by a test). Not ported: searching on into the next file, "selection only" for several lines. |
| `FormResolveConflicts` | `src/app/GitUI/CommandsDialogs/FormResolveConflicts*.cs` | `66050831e` | `CommandsDialogs/ResolveConflictsWindow.axaml`, `GitUI.Presentation/CommandsDialogs/ResolveConflictsDialog.cs` (`ResolveConflictsViewModel`, `IResolveConflictsHost`); `GitUI/AvaloniaHosting/AvaloniaDialogs.ResolveConflicts.cs` | `GitUICommands.StartResolveConflictsDialog` | The conflicted files (sorted, multiple selection, the selection kept on rescan), their local/base/remote (ours/theirs swapped in a rebase) with the submodule hashes; the merge tool as WinForms reads it (kdiff3 compatibility, .exe split), merge with the modified-file check and "Is the merge conflict solved?" or git mergetool, the binary file question, the 2-way fallback, the Diff-Scripts; choose local/remote/base with the counters and "apply to all"; submodule conflicts in the merge submodule dialog; the full context menu (also on Merge) with the custom merge tools; the hotkeys and Ctrl+1/2/3; the end: update submodules, the suppressible "commit now?" and the commit dialog. Deliberate differences: cancelling the "Solve merge conflict" question does nothing (WinForms applied the previous choice); "apply to all" is reset by each choose command; the files are handled in list order; Ctrl+1/2/3 work anywhere in the dialog; the binary file question has the Question icon; long descriptions wrap. The hidden Author column is not ported. |
| `FormPull` | `src/app/GitUI/CommandsDialogs/FormPull*.cs` | `66050831e` | `CommandsDialogs/PullWindow.axaml`, `GitUI.Presentation/CommandsDialogs/PullDialog.cs` (`PullViewModel`, `IPullHost`, `PullStrings`); `GitUI/AvaloniaHosting/AvaloniaDialogs.Pull.cs` (`PullHost`) | `GitUICommands.StartPullDialog`, `StartPullDialogAndPullImmediately` (menus, toolbar, left panel, push and remotes dialogs, `pull` verb) | The constructor (the pull actions, the remote of the branch, the shallow check), the remotes and URL sources with the URL history and the folder picker, the local and remote branches (listed when the list drops down, as `BranchesDropDown`), the merge and tag options with their enabling (`MergeCheckedChanged`, `RebaseCheckedChanged`, `FetchCheckedChanged`, `RemotesValidating`, `Prune_CheckedChanged`, `PruneTags_CheckedChanged`, `localBranch_Leave`), the help images, the title and button, manage remotes, stash, solve conflicts (the content is disabled while the merge tool runs, as `FormBusyScope`), auto stash. `PullChanges` in the view model: the validations, the settings, the merge commit and detached HEAD questions, `CalculateLocalBranch` with its task dialogs, the event scripts, the auto stash and its pop, the submodules, the conflicts. "Pull immediately" (`PullAndShowDialogWhenFailed`, with the fetch and prune all confirmation) runs the same view model without a window, owned by the caller, and shows the dialog only if nothing was pulled. Git runs in the WinForms `FormRemoteProcess`; `CreateFormProcess`, `HandlePullOnExit` (the prune offer, with the `IsRefRemoved` regex), `LoadPuttyKey` and the question of `PopStash` are duplicated in `PullHost`. No position restore, as the WinForms form (`enablePositionRestore: false`). Deliberate differences: the dialog stays open when nothing was pulled (a validation error, a declined question), as `PullClick` intends (the WinForms `Pull` button also has `DialogResult = OK`); the remote and URL are validated on every change, not when leaving the box; the empty remote branch is listed once (`FormPull` inserts another at every drop down); the window sizes to its content and is not resizable (the groups only stretched horizontally). The auto-completion of the remotes (`ComboBoxSuggestAppend`, as `SuggestAppend`); the drop down of an Avalonia combo box is as wide as its items (`ResizeDropDownWidth`); the help images lightened on a dark theme (`HelpImageView`); the Question icon of the "conflicts solved" and "initialize submodules" questions (`IMessageBoxService.ConfirmQuestion`). |

## Phase 8: WinForms removed

The application runs on `Microsoft.NETCore.App` only: no project sets `UseWindowsForms`, and `DisableTransitiveFrameworkReferences`
drops the WPF framework reference of Microsoft.VisualStudio.Threading. Deleted: the WinForms forms, controls and helpers of GitUI,
ResourceManager and GitExtUtils, `ICSharpCode.TextEditor`, `ConEmuWinForms` and WindowsAPICodePack. What replaced the WinForms
pieces that code outside the forms used, with the names and values of the WinForms API so that the callers did not change:

| WinForms | Replacement |
|---|---|
| `MessageBox`, `TaskDialog` | `GitExtensions.Extensibility/Dialogs/NativeMessageBox.cs`, `TaskDialog.cs` (`MessageBoxW`, `TaskDialogIndirect` with comctl32 v6 through an activation context) |
| `OpenFileDialog`, `SaveFileDialog`, `FolderBrowserDialog`, `ColorDialog`, `FontDialog` | `GitExtensions.Extensibility/Dialogs/CommonDialogs.cs` (the Common Item Dialog, `ChooseColorW`, `ChooseFontW`) |
| `Clipboard` | `GitExtUtils/ClipboardUtil.cs` (user32, text and "HTML Format") |
| `Application.UserAppDataPath` and the other paths | `GitExtUtils/ApplicationInfo.cs` (the same algorithm, from the entry assembly) |
| `Keys` | `ResourceManager/Hotkey/Keys.cs` (a copy of the enum) |
| `Timer` | `GitExtUtils/GitUI/UiTimer.cs` (a thread-pool timer posting its ticks to the synchronization context) |
| `Screen`, `TextRenderer` | `GitExtUtils/GitUI/Screens.cs`, `TextMeasurement.cs` |
| WindowsAPICodePack (taskbar, jump list) | `GitUI/Taskbar/NativeTaskbar.cs` (`ITaskbarList3`, `ICustomDestinationList`; the thumbnail buttons through the window procedure of the Avalonia window) |
| `ConEmuControl` | `externals/ConEmuInside` (the session of conemu-inside `769053b` without WinForms, and `ConEmuHost`), in a `NativeHostWindow` child window (`GitUI/ConsoleEmulation/NativeHostWindow.cs`) |
| The WinForms bug report | `BugReporter/BugReportDialog.cs` with `GitUI.Avalonia/BugReporter/BugReportWindow.axaml` |

After an upstream merge, also compare `externals/ConEmuInside` with the upstream conemu-inside sources it was taken from (its
README). The tests pump their messages with `MessageWindowSynchronizationContext` (`CommonTestUtils/MessagePump.cs`); NUnit runs a message loop for
an async STA test only for the WinForms and WPF contexts, recognized by name, so `CommonTestUtils/NUnitMessagePump.cs` declares a
context with the WinForms name. `UI.IntegrationTests/AvaloniaHosting/AvaloniaHostingTests.NativeDialogs.cs` drives the native
dialogs.

The environment information of the bug report lists the `Microsoft.NETCore.App` runtimes (the check for updates still reads
the desktop runtimes, since the upstream releases it offers are WinForms builds). Not yet: the MSI was not built on the development machine
(no Visual Studio msbuild and WiX there), but the publish, `Check-BundlesConsistent.ps1` and the portable archive pass.

## Third-party controls

Adopted after phase 8 to close gaps of the port (all MIT, all built for Avalonia 12; the plugins do not copy them, see
`src/plugins/Directory.Build.targets`, and `setup/installer/Product.wxs` lists them):

| Package | Used for | Where | Notes |
|---|---|---|---|
| AvaloniaEdit.TextMate (TextMateSharp, Onigwrap) | The syntax highlighting of files and diffs, by the grammars and the Light+ / Dark+ themes of Visual Studio Code | `GitUI.Avalonia/Editor/TextMateColorizer.cs`, `TextEditorView` | Replaces the highlighting definitions of AvaloniaEdit (kept for the extensions TextMate does not know). A diff is tokenized as its old file (removed and context lines) and new file (added and context lines), without the prefixes. With git's colors as the background (`ReverseGitColoring`), the changed lines keep the syntax colors (`DiffColorizer.KeepsSyntaxColors`). Not highlighted: lines over 5000 characters, files over 50000 lines, word diffs, git grep. |
| AvaloniaHex | Binary files in hexadecimal and ASCII (as `FileViewer.DisplayAsHexDump`) | `FileViewKind.Binary`, `FileViewerView` (`hexEditor`), `FileViewerHost.GetItem` | All the bytes, read-only, where WinForms showed a text dump of the first 4 KB. |
| Iciclecreek.Avalonia.Terminal (XTerm.NET, Porta.Pty, the ConPTY of Microsoft) | The built-in terminal: the console tab and the progress dialog | `GitUI/ConsoleEmulation/BuiltIn/*` (`BuiltInTerminalEmulator`, `BuiltInTerminal`, `TerminalOutputProcessor`) | The default console emulator (`ConsoleEmulatorName` = `terminal`), and the fallback before ConEmu; ConEmu and Mintty can still be chosen. The console views are `IEmbeddedView`: a native window or an Avalonia control. The processes run in a pseudo console spawned by `BuiltInTerminal` and attached to the control (the control would launch a finished command again when it is loaded), in a job object (they end with the application). Themes: Tomorrow / Tomorrow Night (following the application theme), Solarized Light / Dark. For the cross-platform phase: a shell descriptor for `$SHELL`, `cd` without the Git Bash paths, `Path.PathSeparator` in the PATH of the shell, and the Unix native files of Porta.Pty in the packages. |

Not adopted: TreeDataGrid, whose releases for Avalonia 12 are part of the commercial Avalonia Pro (they validate a license);
only its releases for Avalonia 11 are MIT. The large trees are virtualizing lists of their visible nodes instead
(`GitUI.Avalonia/Controls/FlatTree`: `FlatTreeRows` follows the nodes of a tree, `FlatTreeList` shows them in a `ListBox` with
their indentation, expanders, the keys of a tree and the selection of the view model): the file status list (also the file
tree tab) and the left panel. The small trees (the settings pages, the bug report, the TeamCity build chooser, the statistics
of a plugin) stay `TreeView`s.

## Remaining gaps (audited 2026-09-25)

Every "not yet" of the rows above was checked against the code on 2026-09-25; the rows now say what is really missing. By area:

- **File status list**: none left. (The file type icons of the shell, read in the background for each extension
  (`FileStatusIconImage`); the script hotkeys of the main window get the files of the diff
  tab or the file tree and the line and column at the caret, as `FormBrowse.GetScriptOptionsProvider`.)
  (Done since the audit: git grep with its box, prompt and hotkeys; the toolbar with the settings button and the A/B filter;
  select all and collapse root folders; the custom difftool submenus and the First / Second captions, also in the grid and
  the file history; "Open with Git Extensions" for a submodule, and the double click of the lists without their own action
  (the history of the file, or the submodule if `OpenSubmoduleDiffInSeparateWindow`).)
- **Diff viewer**: none left. (Done since the audit: "(selection only)" in the label of the search box (the panel has no title); the
  range-diff path filter, the search going on into the next files of the diff tab, "selection only"; the continuous scroll into the next / previous file with its menu item, the occurrences of the selection with their
  hotkeys, the first change after loading, the position kept for the same file, go to line by the line of the file. Going
  to an occurrence selects it: AvaloniaEdit drops a selection the caret leaves. `TextEditor.ScrollToVerticalOffset` does not
  scroll, so `TextEditorView` sets the offset of its scroll viewer.)
- **Revision grid**: none left. (Done since the audit: the favicon of the build report tab (read by WebView2 once the report is shown;
  WinForms loaded the report for it without the tab shown); the menu, hotkeys and columns of the grid of `FormLog`; the "select next fork point" hotkey; the avatars loaded again when their cache is cleared; the labels of
  `MessageColumnProvider` with the tracked remote nestled after its branch and the ahead / behind counts as a virtual label
  (in italics: Avalonia borders have no dashes), their tooltips and the double click going to the related branch.)
- **File history**: none left. (The filter toolbar of its grid, the Navigate and View menus, the build report tab with the
  build statuses (`BuildReportViewModel`, shared with the main window). A `MenuFlyout` keeps the items it was first shown
  with, so the menus read when they open are new flyouts (`FreshMenuFlyout`, also for the git grep history).)
- **Stash**: none left. (The hotkeys (next / previous stash, refresh) and the cherry-pick of all changes of the viewer.)
- **Blame**: none left. (The avatars of the authors at the first line of each commit, beside the age bars; the blame in the
  diff and file tree tabs instead of the viewer ("Blame" checked; the diff tab opens it in the file tree tab unless
  `UseDiffViewerForBlame`), `BrowseViewModel.Blame.cs`.)
- **Commit info**: none left. (The menu of the avatar: the provider, the generated style, clearing the cache, gravatar.com;
  the avatar loaded again when the cache is cleared elsewhere.)
- **Left panel**: none left. (Done since the audit: the run script menu, Alt+click to the related branch, the width and
  visibility saved, the icons lightened on a dark theme as WinForms (`ImageLightness`, also for the other images WinForms
  adapted: the help images, the viewer's whitespace buttons, the remotes' eyes, the scripts, the settings and dashboard icons).)
- **Filter toolbar**: none left. (The branch list updated and dropped down while typing, as `tscboBranchFilter_TextUpdate`.)
- **Dashboard**: none left. (The background tiled at its size, the arrow keys moving between the groups, the "Clone fork"
  links of the git hosters added once the plugins are loaded.)
- **Repository switch**: none left. (The revisions to select after a switch.)
- **Pull**: none left. (The auto-completion of the remotes, the help images on a dark theme, the Question icon.)
- **Other**: none left. The environment information of the bug report lists the `Microsoft.NETCore.App` runtimes, which the
  application runs on (the updates check still reads the desktop runtimes, as the upstream releases need them). Deliberately
  not ported: toolbar items of plugins (the plugin API has no toolbar; a WinForms plugin could only reach into the controls
  of `FormBrowse`, and no bundled plugin does), the "containing" mode of the branch selector and the hidden Author column of
  the resolve conflicts dialog (nothing in Git Extensions uses them).

## Layout

The windows follow the layout of their WinForms forms (checked against each Designer.cs in September 2026: the rows, toolbars,
order, group boxes and icons). Deliberate differences:

- The help button of the WinForms title bar is F1 (`DialogWindow.ManualSectionSubfolder` and `ManualSectionAnchorName`).
- The OK/Cancel buttons of a few forms that have them inside their table (`FormCreateTag`, `FormFormatPatch`, `FormMergeBranch`,
  `FormAvailableEncodings`) are in the footer strip, as in the other dialogs.
- Some fixed widths are larger where the Fluent controls need the room (the key id of `FormCreateTag`, the columns of
  `FormViewPatch`, the number boxes of `FormRevisionFilter`).
- `FormStash` has a refresh button for its F5 hotkey.

The options toolbar that the WinForms `FileViewer` shows when the mouse moves over it is ported in `FileViewerView` (next and
previous change, context lines, entire file, nonprinting characters, whitespace, encoding, settings; the options are saved in
`AppSettings` as the WinForms viewer saves them), with the syntax highlighting of diffs and the context menu (copy patch, find,
stage lines, ...). The file editors (`FormEditor`, `.gitattributes`, `.mailmap`, `.gitignore`) have `EditorOptionsToolbar`
(nonprinting characters, settings, and for `FormEditor` the encoding: choosing one reads the file again in it while unchanged,
and the file is saved in it; the WinForms combo box of the editor only changed later reads).

## Translations

Each view's strings class (`GitUI.Presentation/**/*Strings.cs`) declares the XLIFF ids of the form it replaces.
`GitUI.Avalonia.Tests/ViewModels/ViewStringsTests.cs` fails if an id or its English text no longer matches
`English.xlf`. So when an upstream merge changes a form's strings, that test points at what to update.

## Platforms

The cross-platform phase (`CROSS-PLATFORM.md`) records here which parts run on which system. **Builds** means that
`dotnet build` of the solution succeeds there; **runs** that the code is used there, with its tests passing there.

| Area | Windows | Linux | macOS | Notes |
|---|---|---|---|---|
| Build of the solution | builds | builds (CI: `build-linux` of `app-build.yml`) | not checked | Phase 0. Linux needed exact file name case in the project files and `.resx` (`plugin.png`, `puttygen.png`, `..\..\plugins\`), and `DisableTransitiveFrameworkReferences` for `externals/` too (the WPF framework reference of Microsoft.VisualStudio.Threading does not resolve off Windows). |
| Native components (`src/native`), MSI (`setup/installer`) | builds | skipped | skipped | Windows-only by nature: `build.proj` does nothing elsewhere, and `dotnet build` of the solution does not build the WiX project. |
| Publish (`Project.Publish.targets`) | runs | not checked | not checked | Windows PowerShell on Windows, `pwsh` elsewhere; the bundle check against `Product.wxs` and the MSI only on Windows. |
| Test serialization (`TestAppSettingsAttribute`) | runs | runs | not checked | A lock file instead of a named semaphore (not supported on Unix); it is released when a test host is killed. |
| Projects targeting `net10.0` (portable, checked by CA1416) | runs | builds | not checked | `GitExtensions.Extensibility`, `GitExtUtils`, `GitUIPluginInterfaces`, `GitCommands`, `ResourceManager`, `GitUI.RevisionGraph`, `GitUI.Presentation` (all the projects of phase 1); the tests `CommonTestUtils`, `GitExtUtils.Tests`, `GitExtensions.Extensibility.Tests`, `GitUIPluginInterfaces.Tests`, `ResourceManager.Tests`, `GitCommands.Tests` (these run on Linux, about 3700 tests). Tests of Windows paths (drives, UNC, `\\wsl$`, backslashes written with slashes for git), WSL and Cygwin, STA or Win32 are `[Platform(Include = "Win")]` (or `IncludePlatform = "Win"` for a single case); where Linux behaves differently but correctly, a `[Platform(Exclude = "Win")]` test checks it (`FullPathResolverUnixTests`, `FindAncestors_unix`). Set by `<TargetFramework>$(PortableTargetFramework)</TargetFramework>` in the project. `CommonAssemblyInfo.cs` declares Windows only for the `net10.0-windows` projects (`#if WINDOWS`). NUnit skips a test assembly that declares Windows only, so the tests of a project run on Linux once the project and its test project target `net10.0`. |
| Projects targeting `net10.0-windows` | runs | builds, tests skipped | not checked | All the other projects. |
| Fonts of the settings | runs | builds | not checked | `FontDescriptor` (family, size in points, bold, italic) instead of the GDI+ `Font`, saved in the same text format (`FontParser`), so existing settings load unchanged. Defaults: on Windows the message box font (as before) and Consolas; macOS `.AppleSystemUIFont` 13 and Menlo; elsewhere DejaVu Sans 9 and DejaVu Sans Mono (to be checked in phase 5). `GdiFontExtensions` converts for the Windows code that draws with GDI+ (the font dialog, the avatars, the menu captions). Plugin API: `SettingsSource.GetFont` / `SetFont` take a `FontDescriptor`. |
| Common dialogs (`CommonDialogs.cs`) | runs | not available | not available | `[SupportedOSPlatform("windows6.1")]`; only the Windows-only projects use them. Phase 2 moves the callers to `IFileDialogService` and Avalonia pickers. |
| Measuring text (`TextMeasurement`) | runs | estimated | estimated | GDI+ on Windows; elsewhere half the font size per character, until phase 2 measures with Avalonia. Only the shortening of the recent repository captions uses it. |
| Settings in the registry (`HKCU\Software\GitExtensions`) | runs | settings file | settings file | The paths of git and SSH (`gitcommand`, `gitssh`) are in the settings file on every system, copied once from the registry on Windows (`MigrateRegistrySettings`, flag `Hidden.Migration.IsRegistrySettingsMigrated`; the registry is left as it was). What other Windows components read stays in the registry on Windows: `InstallDir`, `CheckSettings` (the installer), `CascadeShellMenuItems`, `AlwaysShowAllCommands` (the shell extension), `ShowCurrentBranchInVS` (the Visual Studio extension); elsewhere they are kept in the settings file. The first import of the old registry settings (`ImportFromRegistry`) only runs on Windows. |
| Data folder (`ApplicationInfo.UserAppDataPath`) | `%APPDATA%` (unchanged) | `$XDG_CONFIG_HOME` or `~/.config` | `~/Library/Application Support` | Joined with the separator of the system; the folders are created when missing (`GetFolderPath` returned nothing for a missing folder off Windows). The settings are in `GitExtensions/GitExtensions/` below it, as on Windows. |
| Win32 helpers of GitExtUtils | runs | not available | not available | `ClipboardUtil` and `Screens` are `[SupportedOSPlatform("windows")]` (only the Windows-only projects use them; phase 2 moves to the Avalonia clipboard and screens). `DpiUtil` and `SystemTheme` return the defaults (96 DPI, light) off Windows. Removed as dead: `GitExtUtils/GitUI/Interops`, `BmpTransformation`, `LightnessCorrection`, `BrushScope`, `ColorHelper.AdaptLightness` (replaced by `ImageLightness`), `DpiUtil.Scale(Image)`. Plain P/Invoke is not checked by CA1416: `NativeMessageBox` and `TaskDialog` (called by the portable `MessageBoxes`) are phase 2. |
| Icons of the shell for files (`FileAssociatedIconProvider`) | runs | not available | not available | `[SupportedOSPlatform("windows6.1")]` (`Icon.ExtractAssociatedIcon`); a capability of phase 4. |
| Saving the settings (`AppSettings.SaveSettings`) | runs | runs | not checked | The named mutex that serializes the saving between processes had the path of the settings file in its name; a name with '/' is invalid on Unix, and the exception was swallowed, so the settings were never saved there. Off Windows the name has a hash of the path (`GetSettingsMutexName`); on Windows it is unchanged. |
| Dead Win32 code | removed | - | - | Also removed in phase 1: 23 unused declarations of `GitUI/Interops` (the list views, rich text, themes, GDI brushes of WinForms), `WebBrowserEmulationMode` (for the WinForms `WebBrowser`), and the analyzer project `GitExtensions.Analyzers.CSharp` (its only generator made default constructors for WinForms `Form` and `UserControl` classes, of which none are left). |
| Images of the plugin API (plugin API v3) | runs | builds | not checked | `PluginImage` (encoded bytes: from bytes, a stream or a manifest resource). `IGitPlugin.IconImage` (a default member, so v1/v2 plugins still build), `PluginMenuItem.IconImage`, `IGitUICommands.RegisterCommitTemplate`; the host shows them rather than the GDI+ `Icon` of v1/v2, which is converted on Windows only (`GdiImageExtensions.ToPluginImage`). The commit templates of GitCommands hold a `PluginImage` (`[IgnoreDataMember]`, as `DataContractJsonSerializer` saves them: the templates of the settings never had one). The in-repo plugins embed their `Resources\Icon*.png` as `PluginIcon.png` (`src/plugins/Directory.Build.props`) and no longer set the GDI+ icon. |
| Avalonia platforms (`AvaloniaUi`) | Win32 | X11 | native (not checked) | Phase 2: `Avalonia.Desktop` and `UsePlatformDetect()` instead of `Avalonia.Win32` and `UseWin32()`. Its assemblies (X11, Native, FreeDesktop, Tmds.DBus) are in `Product.wxs`. The Windows package still keeps only the native files of Windows (`Project.Avalonia.targets`); the packages per system are phase 6. |
| Owners of the dialogs (`AvaloniaDialogHost`) | native owners | Avalonia owners | Avalonia owners (not checked) | The owners stay native handles (`IWin32Window`, `WindowOwner`): off Windows the handle is the one of the platform of an Avalonia window (`DialogWindow.NativeHandle`, e.g. an X11 window), so the dialog is shown with Avalonia's `ShowDialog` over the open window of that handle, in a nested dispatcher frame (the callers get the result when the call returns). This differs from the plan (an `IWindowOwner` replacing `IWin32Window`): no call site changes. A spike showed nested synchronous message boxes over a modal dialog on X11 (WSLg) and Windows. On Windows the native path is unchanged. |
| Message boxes and task dialogs | native | Avalonia | Avalonia (not checked) | `IDialogBoxHost` (`DialogBoxHost.Current`, set by `AvaloniaDialogBoxHost.Register` at startup): `NativeMessageBox` and `TaskDialog` use it off Windows, and on Windows with `GE_AVALONIA_DIALOGBOXES=1`. `MessageBoxWindow` (the buttons, icon and default button, Escape and the close button as the message box of Windows, Ctrl+C) and `TaskDialogWindow` (heading, text with links, command links, standard and custom buttons with `AllowCloseDialog`, verification, expander, footnote) in `GitUI.Avalonia/Hosting/DialogBoxes.cs`; their captions are `DialogBoxStrings` (category `DialogBoxes`). The icons are drawn. |
